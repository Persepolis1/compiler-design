program {
    int x;
    int y;
    int z;
    // 2610
    x = (1 + ((5 * 2 + 3 * 6 - 10 / 5) * 10)) * 10;
    put(x);
    // 5212
    y = 50 + x - 2 * (100 / 2) + 7 * 5 + 3 + 5 - 1 + x;
    put(y);
    // 136033150
    z = (10 * y) * x - 50;
    put(z);
    // 4
    put(1+1*5-2);
    // 1
    put(1 or 1);
    x = (((5 + 10 -13) * 2) / 2) and not -1 >= +100 * (20 + 10/23);
    // 1
    put(x);
    x = (((5 + 10 -13) * 2) / 2) and not -1 > +100 * (20 + 10 and 23);
    // 1
    put(x);
    x = (((5 + 10 -13) * 2) / 2) and not -1 <> +100 * (20 + 10 or 23);
    // 1
    put(x);
    x = (((5 + 10 -13) * 2) / 2) and not -1 <= +100 * (20 + 30 and 23);
    // 0
    put(x);
    x = (((5 + 10 -13) * 2) / 2) and not -1 < +100 * (20 + 30 and 23);
    // 0
    put(x);
    x = 5 <= 2;
    // 0
    put(x);
};
